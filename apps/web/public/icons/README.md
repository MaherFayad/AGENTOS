# Icons — PLACEHOLDER

`icon-192.png`, `icon-512.png` and `icon-maskable-512.png` are **generated placeholders**,
not final artwork: a core dot, one orbit ring and three satellites on `--bg`, drawn
monochrome from the §2.1 galaxy motif.

They exist so the PWA is genuinely installable today (§3.6) rather than "installable once
someone draws an icon". Replace them with real artwork before anyone shows this to a
person who did not build it. Keep: near-black background, ivory mark, artwork inside the
centre 80% for the maskable variant.

Owner: `shell-navigation-engineer`.
