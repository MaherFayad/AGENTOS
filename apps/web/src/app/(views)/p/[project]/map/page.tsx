/**
 * `/map` — the galaxy view (§2.1). The canvas lives in `layout.tsx` so it survives
 * drill-in navigation. This page is an empty slot on purpose.
 */
export default function MapIndexPage(): null {
  return null;
}
