/**
 * `/map/:department` — the drill-in (§2.2). Camera and watermark are driven by the
 * URL inside the layout's `<MapPage />`. This file exists so the route is real.
 */
export default function DepartmentPage(): null {
  return null;
}
