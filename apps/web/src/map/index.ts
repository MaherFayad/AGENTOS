/**
 * MAP — the galaxy and department drill-in (§2.1, §2.2).
 * Public surface of `src/map/**`. Anything not exported here is private.
 */

export { MapPage } from './MapPage';
export { MapView, type MapViewProps } from './MapView';
export { GalaxyCanvas } from './canvas/GalaxyCanvas';
