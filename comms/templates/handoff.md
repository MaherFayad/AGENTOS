---
agent: <your-agent-slug>
milestone: M<n>
spec: <§ numbers you implemented>
created: <yyyy-MM-ddTHH:mm>
status: ready-for-review | accepted
---

# <Milestone> — <what you built>

## What exists now

Paths, not prose. `apps/web/src/features/map/GalaxyCanvas.tsx` — the canvas underlay.

## How to use it

The smallest working example: the import, the props, the route, the env var.

## Contracts touched

Which contract files this depends on, and whether you changed any (ADR link if so).

## Deliberately not done

The parts of the spec § you left out and why. This is the most useful section in the
file — it's how the next agent knows the difference between "missing" and "decided".

## Verification

What you actually ran, and what it printed. Screenshot path if fidelity-relevant.

## Next agent

Who picks this up and what the first thing they should read is.
